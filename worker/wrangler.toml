name            = "looking-up-proxy"
main            = "index.js"
compatibility_date = "2024-09-23"

# Routes configured via Cloudflare Pages _redirects file.
# Wrangler routes are set on the Cloudflare dashboard after deployment.
